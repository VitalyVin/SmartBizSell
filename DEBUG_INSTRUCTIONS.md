# 🔍 Инструкция по отладке сохранения черновиков

## Где посмотреть логи на reg.ru

### Вариант 1: Через панель управления reg.ru

1. Войдите в **панель управления reg.ru** (https://www.reg.ru/user/)
2. Перейдите в раздел **"Хостинг"** → **"Мой хостинг"**
3. Выберите ваш тариф
4. Найдите раздел **"Логи"** или **"Статистика"**
5. Откройте файл **`error_log`** или **`php_error.log`**

### Вариант 2: Через FTP/File Manager

1. Подключитесь к хостингу через **FTP** или **File Manager** в панели reg.ru
2. Перейдите в корневую директорию сайта (обычно `public_html` или `www`)
3. Найдите файл **`error_log`** или **`php_error.log`**
4. Откройте его в текстовом редакторе

**Обычные пути к логам на reg.ru:**
- `/home/u3064951/data/www/smartbizsell.ru/error_log`
- `/home/u3064951/data/logs/error_log`
- `/var/log/php_error.log`

### Вариант 3: Через SSH (если доступен)

```bash
ssh u3064951@smartbizsell.ru
tail -f /home/u3064951/data/www/smartbizsell.ru/error_log
```

## 📊 Визуальная отладка (без доступа к логам)

### Способ 1: Отладочный скрипт

1. Откройте в браузере: **`https://smartbizsell.ru/debug_draft.php`**
2. Вы увидите полную информацию о последнем черновике:
   - Размер `data_json`
   - Содержимое таблиц
   - Статус восстановления данных

### Способ 2: Отладочный блок на странице формы

1. После сохранения черновика добавьте в URL параметр: **`?debug=1`**
   Например: `https://smartbizsell.ru/seller_form.php?form_id=123&debug=1`
2. На странице появится блок с отладочной информацией

### Способ 3: Проверка через базу данных

1. Войдите в **phpMyAdmin** через панель reg.ru
2. Выберите базу данных **`u3064951_SmartBizSell`**
3. Откройте таблицу **`seller_forms`**
4. Найдите последний черновик (статус = `draft`)
5. Посмотрите поле **`data_json`** - там должны быть все данные

**SQL запрос для проверки:**
```sql
SELECT 
    id, 
    asset_name, 
    status, 
    LENGTH(data_json) as json_size,
    SUBSTRING(data_json, 1, 200) as json_preview,
    updated_at 
FROM seller_forms 
WHERE user_id = YOUR_USER_ID 
ORDER BY updated_at DESC 
LIMIT 5;
```

## 🧪 Тестовые сценарии

### Тест 1: Базовое сохранение
1. Откройте форму: `seller_form.php`
2. Введите **только** "Название актива": `Тест 1`
3. Нажмите **"Сохранить черновик"**
4. Проверьте:
   - URL содержит `?saved=1&form_id=...`
   - Появилось сообщение "Черновик сохранён"
   - Откройте `debug_draft.php` - должно быть `asset_name: "Тест 1"`

### Тест 2: Сохранение с таблицей
1. Откройте форму
2. Введите "Название актива": `Тест 2`
3. В таблице "Объемы производства" в первой строке введите:
   - Вид продукции: `Продукт А`
   - Ед. изм.: `шт`
   - 2022 факт: `100`
4. Нажмите **"Сохранить черновик"**
5. После перезагрузки проверьте:
   - В таблице должно быть "Продукт А"
   - В `debug_draft.php` должно быть `production: EXISTS (1 items)`

### Тест 3: Проверка Network (DevTools)
1. Откройте **DevTools** (F12)
2. Перейдите на вкладку **Network**
3. Заполните форму и нажмите **"Сохранить черновик"**
4. Найдите запрос к `seller_form.php`
5. Откройте вкладку **Payload** или **Form Data**
6. Проверьте, есть ли там:
   - `asset_name=Тест`
   - `production[0][product]=...`
   - `production[0][unit]=...`

## ❓ Что проверить, если не работает

### Проблема: Данные не сохраняются
- ✅ Проверьте `debug_draft.php` - размер `data_json` должен быть > 0
- ✅ Проверьте Network в DevTools - данные должны отправляться
- ✅ Проверьте логи сервера на ошибки PHP

### Проблема: Данные сохраняются, но не восстанавливаются
- ✅ Проверьте `debug_draft.php` - должен быть `JSON валидный: ✅ Да`
- ✅ Проверьте, что `production` есть в списке ключей
- ✅ Проверьте логи - должно быть `HYDRATING FORM - production data: EXISTS`

### Проблема: Сообщение "Черновик сохранён" не появляется
- ✅ Проверьте URL - должен быть `?saved=1`
- ✅ Проверьте, что редирект происходит (Network → Status 302)
- ✅ Откройте консоль браузера (F12) - нет ли ошибок JavaScript

## 📝 Что сообщить при обращении за помощью

Если проблема не решается, соберите следующую информацию:

1. **Скриншот `debug_draft.php`** - полная страница
2. **Скриншот Network (DevTools)** - запрос к `seller_form.php` с Payload
3. **Последние 20 строк из `error_log`** (если доступен)
4. **Результат SQL запроса:**
   ```sql
   SELECT id, asset_name, LENGTH(data_json) as size, updated_at 
   FROM seller_forms 
   WHERE status = 'draft' 
   ORDER BY updated_at DESC 
   LIMIT 3;
   ```

## 🔧 Быстрая проверка через консоль браузера

Откройте консоль (F12) и выполните:

```javascript
// Проверить, есть ли форма
console.log('Form exists:', document.querySelector('form.seller-form') !== null);

// Проверить, есть ли поля production
console.log('Production inputs:', document.querySelectorAll('input[name^="production["]').length);

// Проверить, есть ли скрытое поле form_id
console.log('Form ID:', document.querySelector('input[name="form_id"]')?.value);
```

